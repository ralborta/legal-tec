[phases.setup]
# Node.js version se especifica en .nvmrc (20.18.1)
# Railway/Nixpacks lo detecta automáticamente
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["rm -rf node_modules .npm", "npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "NODE_ENV=production node dist/index.js"

